{
  "name": "projectsanteri",
  "version": "0.0.1",
  "description": "Text based adventure.",
  "author": "Santeri Nurmi, Ramon Brand",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc",
    "start": "node dist/index.js",
    "develop": "npm run build && npm run start",
    "document": "rm -rf doc/dependencies.svg && mkdir -p doc && madge src/index.ts -i doc/dependencies.svg"
  },
  "dependencies": {
    "color": "^3.1.2"
  },
  "devDependencies": {
    "@types/node": "^12.0.4",
    "madge": "^3.4.4",
    "typescript": "^3.5.1"
  }
}
